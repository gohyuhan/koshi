[package]
name = "tile-test-support"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tile-core = { version = "0.1.0", path = "../tile-core" }
tile-pty = { version = "0.1.0", path = "../tile-pty" }
