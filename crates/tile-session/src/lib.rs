//! `tile-session` — workspace model: sessions, tabs, active tab, focus history,
//! pane registry references, and session-level commands.

pub mod error;
pub mod types;

pub mod client;
pub mod session;
